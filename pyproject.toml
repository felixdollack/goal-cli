[project]
name = "goal-cli"
version = "0.1.0"
description = "Simple command-line Goal-Setting & Progress Tracker"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pytest>=9.0.2",
]
